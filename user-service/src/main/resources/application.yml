server:
  port: 9000

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/saloon_booking_db
    username: root
    password: mysql
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect
